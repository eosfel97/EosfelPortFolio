---
// Redirect to /fr or /en based on Accept-Language, fallback EN
const acceptLang = Astro.request.headers.get('accept-language') ?? '';
const prefersFr = acceptLang.toLowerCase().includes('fr');
const target = prefersFr ? '/fr' : '/en';

return Astro.redirect(target, 302);
---
