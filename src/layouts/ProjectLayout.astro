---
import BaseLayout from './BaseLayout.astro';
import type { Locale } from '../consts';

interface Props {
  title: string;
  description: string;
  lang: Locale;
  canonicalUrl?: string;
  ogImage?: string;
  alternateUrls?: { lang: Locale; url: string }[];
}

const { title, description, lang, canonicalUrl, ogImage, alternateUrls } = Astro.props;
---

<BaseLayout
  title={title}
  description={description}
  lang={lang}
  canonicalUrl={canonicalUrl}
  ogImage={ogImage}
  alternateUrls={alternateUrls}
>
  <header>
    <slot name="header" />
  </header>

  <main id="main-content">
    <article>
      <slot />
    </article>
  </main>

  <footer>
    <slot name="footer" />
  </footer>
</BaseLayout>
